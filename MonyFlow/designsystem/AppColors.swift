import SwiftUI

enum AppColors {
    static let background = Color(.systemBackground)
    static let secondaryBackground = Color(.secondarySystemBackground)
    static let tertiaryBackground = Color(.tertiarySystemBackground)
    
    static let primary = Color(red: 0.20, green: 0.40, blue: 0.85)
    static let accent = Color(red: 1.00, green: 0.55, blue: 0.10)
    
    static let income = Color(red: 0.15, green: 0.70, blue: 0.40)
    static let debt = Color(red: 0.90, green: 0.25, blue: 0.30)
    static let warning = Color(red: 0.95, green: 0.65, blue: 0.10)
    
    static let primaryText = Color(.label)
    static let secondaryText = Color(.secondaryLabel)
}
